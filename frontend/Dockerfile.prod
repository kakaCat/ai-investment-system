# 生产环境Dockerfile - 使用nginx部署
FROM nginx:latest

# 删除nginx默认页面
RUN rm -rf /usr/share/nginx/html/*

# 复制构建产物到nginx目录
COPY dist /usr/share/nginx/html

# 复制nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

# 暴露80端口
EXPOSE 80

# 启动nginx
CMD ["nginx", "-g", "daemon off;"]
