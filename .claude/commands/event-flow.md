# 事件流程分析

分析事件系统的数据流、触发链和影响范围。

---

请分析 $ARGUMENTS 的事件流程：

## 输入说明
- 事件类型: `政策事件` - 分析该类型事件流程
- 功能: `事件创建` - 分析该功能的完整流程
- 空: 分析整个事件系统

## 分析内容

### 1. 数据流分析
- 事件从哪里进入系统
- 经过哪些处理环节
- 最终存储在哪里
- 如何被查询和展示

### 2. 触发链分析
- 事件创建触发什么
- AI 分析何时触发
- 通知何时发送
- 关联数据如何更新

### 3. 模块依赖
- 涉及哪些 Service
- 涉及哪些 Repository
- 外部 API 依赖
- 前端组件依赖

### 4. 状态流转
- 事件的生命周期
- 各状态的含义
- 状态转换条件

## 输出格式

```
## 事件系统流程分析

### 📋 分析范围
- **分析目标**: {目标}
- **涉及模块**: X 个
- **关键路径**: X 个

---

## 1. 系统架构概览

```
┌─────────────────────────────────────────────────┐
│                    用户界面                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 事件列表  │  │ 事件详情  │  │ 事件创建  │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────┐
│                    API 层                        │
│  POST /event/list  /event/detail  /event/create │
└───────┬─────────────┬─────────────┬─────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────┐
│                  Service 层                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ListService│ │DetailSvc │  │CreateSvc │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┘
        │             │             │
        ▼             ▼             ▼
┌─────────────────────────────────────────────────┐
│                Repository 层                     │
│           EventRepository                        │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│              PostgreSQL - events 表              │
└─────────────────────────────────────────────────┘
```

---

## 2. 核心流程

### 2.1 事件创建流程

```
用户提交表单
      │
      ▼
┌─────────────┐
│ event/create │ API 入口
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 参数校验     │ Pydantic 验证
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 权限检查     │ 验证 user_id
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 去重检查     │ idempotency_key
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 保存事件     │ EventRepository.create()
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       ▼              ▼
┌─────────────┐ ┌─────────────┐
│ 触发AI分析   │ │ 关联股票     │ 异步任务
└──────┬──────┘ └──────┬──────┘
       │              │
       ▼              ▼
┌─────────────┐ ┌─────────────┐
│ 保存分析结果 │ │保存关联关系  │
└──────┬──────┘ └─────────────┘
       │
       ▼
┌─────────────┐
│ 发送通知     │ 如果影响重大
└─────────────┘
```

**关键代码路径**:
1. `backend/app/api/v1/event_api.py:create_event` (入口)
2. `backend/app/services/event/create_service.py:CreateEventService` (业务)
3. `backend/app/repositories/event_repo.py:create` (存储)
4. `backend/app/services/event/analyze_service.py:AnalyzeEventService` (AI)

---

### 2.2 AI 影响分析流程

```
事件创建完成
      │
      ▼
┌─────────────┐
│ 提取事件信息 │ 标题、描述、类型
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 获取关联股票 │ 从事件或用户持仓
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 获取股票信息 │ 行业、市值等
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 构建 Prompt  │ 事件 + 股票上下文
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 调用 DeepSeek│ AI 分析
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 解析 AI 响应 │ JSON 结构化
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 保存分析结果 │ event_analyses 表
└──────┬──────┘
       │
       ▼
┌─────────────┐
│ 更新事件状态 │ analyzed = true
└─────────────┘
```

**分析结果结构**:
```json
{
  "impact_score": 4,
  "confidence": 0.85,
  "summary": "...",
  "impacts": {
    "short_term": {"score": 4, "description": "..."},
    "mid_term": {"score": 3, "description": "..."},
    "long_term": {"score": 2, "description": "..."}
  },
  "affected_stocks": [
    {"symbol": "600000", "impact": "positive", "reason": "..."}
  ],
  "suggestions": ["..."]
}
```

---

## 3. 状态流转

### 事件生命周期

```
┌─────────┐     ┌─────────┐     ┌─────────┐
│ PENDING │ ──▶ │ANALYZING│ ──▶ │ANALYZED │
└─────────┘     └─────────┘     └────┬────┘
                     │               │
                     ▼               ▼
               ┌─────────┐     ┌─────────┐
               │  ERROR  │     │ NOTIFIED│
               └─────────┘     └─────────┘
```

| 状态 | 含义 | 触发条件 |
|------|------|----------|
| PENDING | 待分析 | 事件创建后 |
| ANALYZING | 分析中 | AI 开始处理 |
| ANALYZED | 已分析 | AI 分析完成 |
| ERROR | 分析失败 | AI 调用失败 |
| NOTIFIED | 已通知 | 通知发送后 |

---

## 4. 数据模型关系

```
┌─────────────┐
│    users    │
└──────┬──────┘
       │ 1:N
       ▼
┌─────────────┐       ┌─────────────┐
│   events    │──────▶│event_analyses│
└──────┬──────┘  1:1  └─────────────┘
       │
       │ N:M
       ▼
┌─────────────┐
│event_stocks │ (关联表)
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   stocks    │
└─────────────┘
```

**关键字段**:
- `events.idempotency_key`: 防重复
- `events.status`: 状态流转
- `event_analyses.impact_score`: AI 评分
- `event_stocks.impact_type`: 影响类型

---

## 5. 触发链分析

### 事件创建触发链

```
事件创建
   │
   ├─▶ [同步] 保存到数据库
   │
   ├─▶ [异步] AI 影响分析
   │      │
   │      └─▶ 保存分析结果
   │           │
   │           └─▶ [条件] 影响 >= 4
   │                  │
   │                  └─▶ 发送通知
   │
   └─▶ [异步] 更新关联股票
          │
          └─▶ 更新股票事件计数
```

### 依赖服务

| 服务 | 依赖 | 类型 |
|------|------|------|
| CreateEventService | EventRepository | 内部 |
| CreateEventService | StockRepository | 内部 |
| AnalyzeEventService | DeepSeek API | 外部 |
| NotifyService | 邮件/推送服务 | 外部 |

---

## 6. 前端组件关系

```
pages/event/
├── index.vue (列表页)
│   ├── EventFilter.vue
│   ├── EventList.vue
│   │   └── EventCard.vue
│   └── EventPagination.vue
│
├── [id].vue (详情页)
│   ├── EventHeader.vue
│   ├── EventAnalysis.vue
│   │   ├── ImpactScore.vue
│   │   └── AffectedStocks.vue
│   └── EventTimeline.vue
│
└── create.vue (创建页)
    ├── EventForm.vue
    └── StockSelector.vue
```

---

## 7. API 调用关系

```typescript
// 前端 Service
// frontend/src/services/event.ts

// 列表查询
POST /api/v1/event/list
  └─▶ EventListService
       └─▶ EventRepository.query_by_user()

// 详情查询
POST /api/v1/event/detail
  └─▶ EventDetailService
       ├─▶ EventRepository.get_by_id()
       └─▶ EventAnalysisRepository.get_by_event()

// 创建事件
POST /api/v1/event/create
  └─▶ CreateEventService
       ├─▶ EventRepository.create()
       └─▶ [async] AnalyzeEventService.analyze()

// AI 分析（手动触发）
POST /api/v1/event/analyze
  └─▶ AnalyzeEventService
       └─▶ DeepSeek API
```

---

## 8. 性能关注点

| 环节 | 耗时 | 优化建议 |
|------|------|----------|
| 事件列表查询 | ~50ms | 添加索引 |
| 事件详情查询 | ~30ms | 缓存热点 |
| AI 分析 | ~3-5s | 异步处理 |
| 通知发送 | ~1s | 消息队列 |

---

## 9. 错误处理

| 错误场景 | 处理方式 |
|----------|----------|
| AI 分析超时 | 重试 3 次，标记 ERROR |
| AI 返回格式错误 | 解析失败，人工处理 |
| 关联股票不存在 | 跳过，记录日志 |
| 重复事件 | 返回已存在事件 |

---

## 📋 优化建议

### 高优先级
1. [ ] AI 分析改为消息队列异步处理
2. [ ] 添加事件列表缓存
3. [ ] 实现分析结果预热

### 中优先级
4. [ ] 添加事件批量导入
5. [ ] 实现分析结果版本控制
6. [ ] 添加分析质量反馈机制

### 低优先级
7. [ ] 事件相似度检测
8. [ ] 跨用户事件共享
```

## 使用示例
- `/event-flow` - 分析整个事件系统
- `/event-flow 事件创建` - 分析事件创建流程
- `/event-flow 政策事件` - 分析政策事件的完整流程
